import React from 'react';
import CategoryList from './CategoryList';

const SidebarLeft = () => {

    return (
        <div>
            <CategoryList/>
        </div>
    );
};

export default SidebarLeft;